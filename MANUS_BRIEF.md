# 桌面便签 · 每日待办小程序 — Manus 代建 Brief

> 自包含说明书。Manus 读这一份就够了，不用问其他上下文。

---

## 一、项目背景

我（Libo）是个内容创业者，主力工具是 Obsidian 知识库（vault）和 Claude Code。
现在需要一个**Windows 桌面常驻便签**，用来记每天的战术级 todo（"下午3点回复呱哥微信"这种当日一次性事项）。

**为什么不用现成的便签 App**：我希望数据写进我的 Obsidian vault（`F:\Obsidian\Libo\daily_todo\`），让每天的 todo 自动成为我知识库的一部分，未来能搜索、双链、跨日复盘。现成 App 都是各自封闭的数据格式，不行。

---

## 二、核心功能

### 主窗口
1. **开机自启动**，常驻桌面**右上角**
2. **按"天"记录**待办，每条一行，**回车键新增**一条
3. **点击条目** → 划掉（strikethrough，标记为完成）；**再点** → 取消划掉
4. **右键条目** → 弹出删除菜单
5. **跨天自动归档**：第二天打开 App 自动新建当日清单，昨天的存档保留可查
6. **顶部显示**：当前日期 + 当日完成率（如"3/7 已完成"）
7. **快捷键 `Ctrl+Alt+N`**：全局热键，唤出窗口到最前 + 焦点跳到输入框
8. **历史回看**：点击顶部日期 → 弹出日历，选某一天 → 加载该日 todo（只读模式）

### 窗口外观与行为
- **无边框 + 半透明（约 90% 不透明度）**
- **始终置顶**（always on top），但**不抢焦点**（我打字时光标不会被跳走）
- **可拖动**：标题栏可拖动移动位置，**记住上次关闭时的位置**（下次启动恢复）
- **默认尺寸 280×400 px**，贴右上角
- **右上角按钮**：最小化到系统托盘（**不要直接关闭进程**），托盘图标右键可"显示窗口/退出"
- **视觉风格**：极简黑白，参考 Things 3 / macOS 备忘录风格（不要花哨的渐变和阴影）

---

## 三、数据存储（重要 · 必须严格遵守）

### 路径
```
F:\Obsidian\Libo\daily_todo\YYYY-MM-DD.md
```
- 每天一个 .md 文件，文件名 = 日期（如 `2026-04-27.md`）
- 文件夹已存在，直接写入即可

### 文件格式（Obsidian 兼容 markdown，必须严格按此结构）
```markdown
---
date: 2026-04-27
done: 2/5
tags: [daily_todo]
---
- [ ] 未完成的待办
- [x] 已完成的待办
- [ ] 又一条未完成的
```

### 字段说明
- frontmatter `date`：当天日期，YYYY-MM-DD 格式
- frontmatter `done`：分子=已完成数 / 分母=总数（每次勾选/新增/删除时实时更新）
- frontmatter `tags`：固定 `[daily_todo]`，用于 Obsidian 全库搜索
- 正文：标准 markdown checkbox 语法（`- [ ]` 未完成 / `- [x]` 完成）

### 读写规则
- 启动时读取 `daily_todo/今天日期.md`，不存在则创建空文件（仅含 frontmatter）
- 每次操作（增/删/勾选/取消勾选）→ **立即同步写回文件**（不要批量延迟写）
- 新增条目追加到文件末尾，划掉/取消划掉只切换 `[ ]` ↔ `[x]`，删除直接移除该行

---

## 四、源码与配置

### 源码路径
```
F:\Obsidian\Libo\copilot\desktop-todo\
```
（文件夹已建好，把所有源码 + 打包脚本放这里）

### 用户配置文件（窗口位置等）
```
F:\Obsidian\Libo\copilot\desktop-todo\config.json
```
存窗口最后位置、尺寸、是否最小化到托盘等用户偏好。

### 开机自启
- 打包成 `desktop-todo.exe`
- 在 `shell:startup`（即 `%AppData%\Microsoft\Windows\Start Menu\Programs\Startup\`）放一个快捷方式
- 安装脚本里自动完成这一步

---

## 五、技术栈建议（可自由选择）

**首选：Python + PyQt6 / PySide6**
- 理由：半透明、无边框、系统托盘、全局热键都有成熟方案
- 打包用 PyInstaller → 单 .exe

**备选：Electron**
- 如果你更熟前端栈，OK
- 注意单 .exe 体积会大很多（>100MB）

**不推荐**：Tkinter（半透明和托盘支持差）、AutoHotkey（UI 能力不够）

---

## 六、交付物清单

1. ✅ **可双击运行的 `desktop-todo.exe`**（单文件，免安装）
2. ✅ **完整源码**（放在 `copilot/desktop-todo/` 下，方便我后续让 Claude Code 帮忙改）
3. ✅ **README.md**：
   - 怎么安装（拷贝 exe + 设置开机自启的一键脚本）
   - 怎么修改源码后重新打包
   - 已知问题 / 限制
4. ✅ **install.bat**：一键脚本，把 .exe 放进 `shell:startup` 完成开机自启
5. ✅ **uninstall.bat**：卸载脚本

---

## 七、视觉风格细节

- **背景色**：纯黑（#1a1a1a）或纯白（#ffffff），二选一，让我能切换
- **文字**：未完成=正常颜色，已完成=灰色 + strikethrough 删除线
- **字体**：系统默认（Windows 下用"微软雅黑"或"Segoe UI"）
- **按钮/图标**：极简线条图标，不要 emoji
- **过渡动画**：勾选时轻微淡出，不要花哨

---

## 八、不要做的事

- ❌ **不要**联网同步 / 上传任何数据 — 纯本地
- ❌ **不要**搞云账号登录 / 数据库
- ❌ **不要**加广告 / 分析埋点
- ❌ **不要**用专有数据格式（必须是普通 .md 文件，能被 Obsidian 直接读）
- ❌ **不要**默认打开自动更新检查
- ❌ **不要**写 emoji 进 markdown 文件（除非用户自己输入）

---

## 九、验收标准

我会按这个清单测：

- [ ] 双击 .exe 启动，窗口出现在桌面右上角，半透明
- [ ] 输入"测试1"+回车，看到新条目；点击它，划掉；再点取消划掉
- [ ] 右键条目能删除
- [ ] 关掉 App，重启电脑，开机后自动出现
- [ ] 打开 `F:\Obsidian\Libo\daily_todo\2026-04-27.md`（用记事本），能看到正确的 markdown 结构
- [ ] 用 Obsidian 打开 vault，搜索 `tag:#daily_todo` 能搜到该文件
- [ ] 按 `Ctrl+Alt+N`，无论当前在做什么，App 窗口都能跳出来 + 焦点到输入框
- [ ] 跨天测试：手动改系统日期到第二天，重启 App，看到空白新清单（昨天的还在）

---

## 十、有问题可以问我

如果某个需求不清楚或有更优方案，**直接问我**，不要自己瞎猜默认行为。
特别是：
- 数据格式有疑问 → 先确认（这块我要求严格）
- 技术栈选型有顾虑 → 提出来
- 视觉风格有更好建议 → 给我截图参考

---

*Brief 版本：v1.0 · 2026-04-27*
*由 Libo + Claude Code 联合产出，参考 Libo Obsidian vault 的现有项目编号系统（P1-P7）和文件路径约定。*
